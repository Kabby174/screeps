var ActionManager = {
  HARVEST: "HARVEST",
  run: (creep, actions = []) => {
    let index = 0;
    // while(task = actions.unshift()){
      // console.log(index,creep.name,task);
      // index++;
    // }
  }
}

module.exports = ActionManager;
